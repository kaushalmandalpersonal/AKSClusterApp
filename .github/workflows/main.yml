name: Build a Docker image and Push it to ACR

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: azure/docker-login@v1
        with:
           login-server: akscontainerregistry2022.azurecr.io
           username: AKSContainerRegistry2022
           password: Cp8em6BfhONo=qGP5x3Seou=vjoMi5y1
      - run: |
          docker build . -t akscontainerregistry2022.azurecr.io/azurewebapptoaks:${{ github.sha }}
          docker push akscontainerregistry2022.azurecr.io/azurewebapptoaks:${{ github.sha }}
